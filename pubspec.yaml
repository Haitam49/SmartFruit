name: smartfruitapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  
  # Core dependencies
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  http: ^1.1.2
  
  # UI & Media
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  image: ^4.1.7
  
  # AI & Machine Learning
  tflite_flutter: ^0.12.1
  # tflite_flutter_helper removed as it causes version conflicts and is not used
  
  # Voice Assistant
  speech_to_text: ^7.3.0
  flutter_tts: ^4.0.2

  # Firebase (Conserved from previous state)
  firebase_core: ^3.4.0
  firebase_auth: 5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cnn_model.tflite
